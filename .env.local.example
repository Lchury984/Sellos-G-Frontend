# Frontend env example
# Use the MSW mock service worker (intercepts network calls)
VITE_USE_MOCK=msw
VITE_API_URL=http://localhost:4000/api
VITE_FRONTEND_URL=http://localhost:5173

# To use the simple JS mock (authService.mock.js) instead, set:
# VITE_USE_MOCK=true
